# Example environment variables for EvidentiaAI
# Copy to `.env` and fill values before running the app.
PUBMED_EMAIL=you@example.com
# Optional: OpenAI API key for future phases
OPENAI_API_KEY=
# Optional: NCBI API key to increase rate limits
NCBI_API_KEY=
